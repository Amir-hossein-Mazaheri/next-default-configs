import { createSlice } from "@reduxjs/toolkit";

const YOUR_REDUCER_NAME = createSlice({
    name: 'YOUR_REDUCER_NAME',
    initialState: {},
    reducers: {}
})

export default YOUR_REDUCER_NAME.reducer;

export const actions = YOUR_REDUCER_NAME.actions;